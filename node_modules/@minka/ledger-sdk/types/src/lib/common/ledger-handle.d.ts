/**
 * Represents a ledger record identifier given by record creator.
 * Handle can consist of lowercase and uppercase letters, digits,
 * and special characters: _, -, :, ., =, $, /, @. Handle should not
 * start with `$<3 letters>.` prefix because this pattern is
 * designated for LUID.
 */
export type LedgerHandle = string;
