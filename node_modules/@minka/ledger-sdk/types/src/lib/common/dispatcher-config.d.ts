import { EventSignal } from '../records/ledger-event';
export declare enum RecordName {
    Bridge = "bridge",
    BridgeEntry = "bridge-entry",
    Circle = "circle",
    Effect = "effect",
    Policy = "policy",
    Intent = "intent",
    Ledger = "ledger",
    Signer = "signer",
    Symbol = "symbol",
    Schema = "schema",
    Wallet = "wallet",
    Request = "request",
    Anchor = "anchor"
}
export type DispatcherEvent = {
    signal: EventSignal;
};
export type DispatcherConfig = {
    recordName: RecordName;
    events: {
        created: DispatcherEvent;
        updated: DispatcherEvent;
        dropped?: DispatcherEvent;
    };
};
